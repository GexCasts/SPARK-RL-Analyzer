# SPARK Overlay

`SPARK_Overlay.html` is the lightweight OBS/browser-source overlay.

It connects directly to the local SPARK Live API bridge and reads overlay names, series length, and series scores from the same local settings used by the analyzer.
