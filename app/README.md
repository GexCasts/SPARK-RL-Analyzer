# SPARK App

This folder is the landing zone for the main analyzer UI as SPARK moves away from a single large HTML file.

For now, `../SPARK.html` remains the production analyzer so existing replay parsing and coaching screens keep working while the app is split into smaller pieces.
